# Server listen port
PORT=3001

# Shared secret for HMAC-OTP stream cipher (64-char hex = 256-bit key)
# Generate one with: node -e "console.log(crypto.randomBytes(32).toString('hex'))"
HMAC_SECRET_HEX=

# Allowed CORS origin (lock down in production, e.g. http://localhost:5173)
CORS_ORIGIN=*
